// A4
/*
 window.alert('My first mensage')
// or
 alert('lest learn js')

 confirm('are you enjoying js')

 prompt('What's  your name')
  name=prompt("what's your name")
 document.write(`O seu nome é ${name}`)

*/
// B
// (modo antigo) -----------------------------------------------------------------------------------

// document.write(`A soma dos valores` + variavel) serve para concatenar grudar um ao outro
//  e se vc colocar Number.paerseInt depois do = no var, vai somar pois a string vai virar number

// (number + number) para adição  + 
// (string + string ) para concatenação   String()
// mas é melhor para mim usar só o + antes do prompt
// -------------------------------------------------------------------------------------------------

// .replace('oque','por oque') serve para mudar o oque(ex: '.') pelo o por oque(ex: ',')

// 5**2 -> 25  (potência)

// ------------------------------------------------------------------------------------------------

// DOM Modulo C

//  utiliza html, css e java script
/*seleciona por:
marca  -> getElementsByTagName('p' ou 'div' ou 'h1' ...)[o numero de qual 'p' ou 'div' ou 'h1' ... 
se tiver mais de um]
ID  # ->  getElemtById()
Name -> getElemntsByName()
Class . -> getElemntsByClassName()
Selctor -> querySelector() || querySelectorAll() - document.querySelector('div#msg')
*/ 
